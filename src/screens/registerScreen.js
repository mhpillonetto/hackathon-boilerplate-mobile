import React from 'react';
import { View, Text } from 'react-native';

const RegisterScreen = props => {
    return (
        <View style={{top: 400, left: 170}}>
           <Text>Sou o Registro</Text> 
        </View>
    )
}

export default RegisterScreen;